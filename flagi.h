#ifndef _FLAGI_H_
#define _FLAGI_H_

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int flag_s( char * );
int flag_a( char * ):
int flag_n( char * );
int error_control( int, int, int, int, int, int );
int added_n ( int );
int added_a ( int );
int added_s (int, int );
int spr_out (char * );
int spr_open (char * );
int spr_in (char * );

#endif
